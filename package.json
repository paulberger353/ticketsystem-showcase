{
  "name": "ticketsystem",
  "version": "1.0.0",
  "description": "Static ticket management demo (Cloudflare Pages, no backend)",
  "scripts": {
    "dev": "wrangler pages dev public/",
    "deploy": "wrangler pages deploy public/"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
